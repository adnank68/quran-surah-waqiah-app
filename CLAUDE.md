# ختم سوره واقعه (quran-surah-waqiah-app)

اپ اندروید نیتیو (Kotlin/Compose) برای ختم سوره واقعه. قراردادهای مشترک همه‌ی
پروژه‌های این مجموعه (یک ریپو به‌ازای هر اپ، نبودن SDK محلی، قیدهای کافه‌بازار)
در `g:\myapps\game\CLAUDE.md` است؛ این فایل فقط چیزهای مخصوص همین پروژه را دارد.

## وضعیت — ۲۰۲۶-۰۹-۰۲

ساخته‌شده از صفر در همین تاریخ. **ریپو قبلاً یک اسکلت React Native بود که اصلاً
قابل بیلد نبود** (نه پوشه‌ی `android/`، نه `index.js`، نه navigation، نه متن
واقعی قرآن) و کاملاً حذف و با پروژه‌ی Android نیتیو جایگزین شد. اگر کسی سراغ
تاریخچه‌ی گیت رفت، آن کامیت‌های اولیه ربطی به اپ فعلی ندارند.

زبان: کد و کامنت‌ها و مستندات **فارسی**، مثل `jadval-emrooz` و `dino` (نه مثل
`sudoku-pro` که انگلیسی است).

## درسِ اصلی این پروژه: محتوای مذهبی را از منبع بیاور، ننویس

هیچ متن قرآنی، ترجمه، تفسیر، روایت یا دستور ختمی در این اپ نوشته‌ی مدل نیست.
همه از منابع بیرونی گرفته و نقل شده‌اند و منبع هرکدام هم داخل اپ و هم در
`tools/README.md` ثبت است. این ادامه‌ی مستقیمِ درسِ
`برنامه-ریزی-و-دستیار-شمسی` است (آنجا تبدیل تاریخ و مناسبت‌ها و موتور استخاره‌ی
تولیدشده با AI همگی غلط بودند).

اگر بعداً محتوایی اضافه می‌شود: **اول منبع، بعد متن.**

- **چیزی که پیدا نشد:** کاربر «حاج آقا عالی» را هم به‌عنوان منبع روش ختم نام
  برده بود؛ روش مشخص و قابل‌استنادی به این نام پیدا نشد و عمداً چیزی از خودمان
  اضافه نشد. اگر کاربر منبعش را داد، به `KHATM_METHODS` در `tools/build-data.js`
  اضافه شود.
- **تفسیر فارسی API ندارد**: نه `api.alquran.cloud` و نه `api.quran.com` هیچ
  تفسیر فارسی‌ای سرو نمی‌کنند (چک شد). تفسیر نور از صفحه‌های تک‌آیه‌ی
  `wiki.ahlolbait.com` استخراج شد (`div.tafasir-tab-content`، تبِ فعال همان
  تفسیر نور است) و با هش، بخش‌های تکراری یکی شدند: ۹۶ آیه → ۱۸ بخش.

## تلاوت (از ۲۰۲۶-۰۹-۰۳)

**دو قاری، قابل انتخاب توسط کاربر** — `audio/Reciter.kt`:

| قاری | منبع | حجم | نکته |
|---|---|---|---|
| استاد منشاوی (مرتّل) | `everyayah.com/data/Minshawy_Murattal_128kbps/` | ۴٫۸۵ مگابایت | استریو ۱۲۸ بود؛ با `ffmpeg -ac 1 -b:a 64k` نصف شد |
| کریم منصوری | `everyayah.com/data/Karim_Mansoori_40kbps/` | ۶٫۴۷ مگابایت | از قبل مونو ۴۰ کیلوبیت است — **رمزگذاری دوباره نکن**، فقط کیفیت را کم می‌کند |

فایل‌ها در `assets/audio/<folder>/000.mp3 … 096.mp3` می‌نشینند و نام‌گذاری هر
دو قاری **یکسان** است (`000` = بسم‌الله). به همین دلیل عوض کردن قاری فقط مسیر
فایل را عوض می‌کند و هیچ منطق دیگری دست نمی‌خورد. برای افزودن قاری سوم فقط
یک ورودی به `Reciter` و یک پوشه‌ی جدید لازم است.

عوض کردن قاری وسط تلاوت، همان قطعه را با صدای جدید از سر می‌گیرد
(`RecitationPlayer.setReciter`). پخش‌کننده خودش قاری جاری را نگه می‌دارد، چون
رفتن خودکار به آیه‌ی بعد داخل `setOnCompletionListener` است.

اپ آفلاین مانده است.

**چرا یک فایل به‌ازای هر آیه:** هماهنگی متن و صوت بدون هیچ جدول زمان‌بندی‌ای
درست درمی‌آید — آیه‌ی در حال تلاوت همان فایلی است که باز است، و
`setOnCompletionListener` آیه‌ی بعد را باز می‌کند. دریفت معنا ندارد.

`audio/RecitationPlayer.kt`: `MediaPlayer` + مدیریت audio focus (با تماس
ورودی مکث می‌کند و خودکار ادامه نمی‌دهد). `WaqiahContentTest` وجود هر ۹۶ فایل
را داخل APK می‌سنجد.

**محدودیتِ شناخته‌شده:** foreground service ندارد، پس اگر اپ مدت زیادی در
پس‌زمینه بماند ممکن است سیستم پخش را قطع کند. برای پخش پایدار در پس‌زمینه
باید `MediaSessionService` اضافه شود.

**مجوز:** بازنشر این تلاوت‌ها برای انتشار فروشگاهی باید مثل تفسیر نور با
کاربر روشن شود.

## اصلاح رسم‌الخط (۲۰۲۶-۰۹-۰۳)

دو مشکل که فقط با اسکرین‌شات کاربر از دستگاه واقعی دیده شدند و در
`normalizeUthmani` داخل `tools/build-data.js` حل شده‌اند (۱۲ آیه عوض شد):

1. **دایره‌های سفید کنار بعضی کلمه‌ها** — `U+06DF` (صفر گرد کوچک) و `U+06DE`
   (نشان رب‌الحزب، آیه‌ی ۷۵). فونت هر دو را دارد؛ مشکل این است که اندروید
   آن‌ها را به‌جای علامتِ ریزِ بالای حرف، گلیفِ مستقل با عرضِ خودش رندر می‌کند.
   حذف شدند — هیچ حرفی از متن کم نشد.
2. **«ءَا» به‌جای «آ»** — رسم‌الخط Tanzil برای «آ» دنباله‌ی `ء + فتحه + ا`
   می‌نویسد. به `U+0622` تبدیل شد: `ٱلْءَاخِرِينَ` ← `ٱلْآخِرِينَ`.

هر دو تستِ واحد دارند تا برنگردند.

**نکته‌ی فونت:** KFGQPC ارقام عربی و `U+06DD` را دارد ولی پرانتزهای تزئینی
`﴿ ﴾` را **ندارد** — به همین دلیل شماره‌ی آیه در حالت «کنار آیه» یک المان
Compose است، نه متنِ درج‌شده در رشته‌ی آیه.

## داده

`app/src/main/assets/waqiah.json` (~۱۴۸ کیلوبایت) دستی نوشته نشده — با
`tools/build-data.js` ساخته می‌شود. دو فایل میانی (`tools/raw.json` و
`tools/tafsir_noor.json`) کامیت شده‌اند تا بازتولید به بالا بودن سایت‌ها وابسته
نباشد. `build-data.js` پیش از نوشتن، صحت را خودش می‌سنجد (۹۶ آیه، پر بودن هر ۴
ترجمه، پوشش کامل تفسیر، جمع ۱۰۵ و ۴۱ برای دو ختم) و `WaqiahContentTest` همان
چیزی را که واقعاً داخل APK می‌رود دوباره می‌سنجد.

**تله‌ای که خورد و حل شد:** نسخه‌ی عثمانیِ `api.alquran.cloud`، «بسم الله» را
به آیه‌ی اول می‌چسباند. جداسازی با مقایسه‌ی رشته‌ی خام کار نمی‌کند (الف وصل
`ٱ` و الف خنجری)، و اگر فقط حروف پایه را تطبیق بدهی، کسره‌ی آخرِ «ٱلرَّحِيمِ»
سرِ آیه‌ی اول جا می‌ماند. `splitBismillah` در `build-data.js` بعد از تطبیق
حروف پایه، اعرابِ چسبیده را هم برمی‌دارد.

## نسخه‌ها و CI

الگو از `sudoku-pro` گرفته شده و همان ترکیبِ سبزِ آنجاست:
AGP **9.1.1**، Gradle **9.3.1** (AGP 9.1.1 حداقلِ سخت‌گیرانه دارد)، Kotlin
**2.2.10**، Compose BOM **2024.09.00**، JDK **17**، compileSdk/targetSdk 36،
minSdk 24.

- افزونه‌ی `org.jetbrains.kotlin.android` اعمال **نمی‌شود** — AGP 9.x پشتیبانی
  Kotlin را داخل خودش دارد (همان کاری که `sudoku-pro` می‌کند).
- `gradlew` و `gradle/wrapper/` و `debug.keystore` کامیت نمی‌شوند؛ هر سه در CI
  ساخته می‌شوند.
- بدون Room/KSP و بدون Firebase — عمداً، تا سطح خطای بیلد کم بماند. حالت اپ
  کوچک است و در `SharedPreferences` به‌صورت JSON می‌نشیند.
- CI روی هر push به `main` هم artifact می‌گذارد و هم یک GitHub Release می‌سازد.
  ریپو **عمومی** است، پس برخلاف `sudoku-pro` به ریپوی انتشار جداگانه و
  `RELEASE_PAT` نیازی نیست.

## نکته‌های پیاده‌سازی که دوباره کشفشان نکن

- **پیشرفت بر پایه‌ی تلاوت است نه روز.** در ختم چهارده‌روزه روز اول ۱ تلاوت و
  روز چهاردهم ۱۴ تلاوت دارد؛ اگر پیشرفت را روی تعداد روز حساب کنی، تیک روز اول
  و روز چهاردهم یک اثر می‌گذارند. `KhatmPlanTest` این را قفل کرده است.
- **`perDay` قلبِ مدل است.** هر روش ختم فقط یک آرایه‌ی «تعداد تلاوت هر روز»
  است؛ طولش = تعداد روز، جمعش = کل. الگوهای عجیب (۳ بار هر شب ولی ۸ بار شب
  جمعه، پنج هفته) بدون هیچ منطق ویژه‌ای در همین آرایه جا می‌شوند.
- **یادآور**: `AlarmManager` با `setExactAndAllowWhileIdle` و در نبودِ اجازه،
  برگشت به `setAndAllowWhileIdle`. آلارم دقیق در اندروید تکرارشونده نیست، پس
  `ReminderReceiver` بعد از هر شلیک، خودش زنگ فردا را می‌گذارد.
  `nextTriggerMillis` اگر ساعت دقیقاً همین لحظه باشد فردا را برمی‌گرداند —
  وگرنه حلقه‌ی بی‌پایانِ شلیک درست می‌شود (تست دارد).
- **آیکون**: minSdk 24 است و adaptive-icon فقط از API 26 کار می‌کند، پس علاوه
  بر `mipmap-anydpi-v26/`، یک وکتور ساده هم در `mipmap/` هست. اگر آن را برداری،
  روی اندروید ۷ آیکون ندارد.
- **RTL** با `CompositionLocalProvider(LocalLayoutDirection provides Rtl)` در
  `MainActivity` قفل شده، نه با زبانِ دستگاه — اپ همیشه فارسی است.
- **متن آیه وسط‌چین، متن دعا از راست‌چین** — دعاها متن پیوسته‌اند و وسط‌چین
  کردنشان بد خوانده می‌شود (`ArabicText(align = …)`).

## کارهای باقی‌مانده پیش از انتشار روی کافه‌بازار

۱. `applicationId` الان `ir.almasbu.waqiah` است — بعد از اولین انتشار
   غیرقابل‌تغییر می‌شود، پس باید تأیید نهایی کاربر را گرفت.
۲. کلید امضای واقعی ساخته نشده؛ **نسخه‌ی release هم فعلاً با کلید دیباگ امضا
   می‌شود** تا APK قابل نصب باشد. برای انتشار واقعی باید یک keystore دائمیِ
   جدا ساخته شود و در secretها بنشیند.

## امضا و شماره‌ی نسخه (۲۰۲۶-۰۹-۰۳)

**کلید دیباگ حالا ثابت است.** قبلاً workflow در هر اجرا با
`keytool -genkeypair` کلید تازه می‌ساخت، یعنی هر APK امضای متفاوتی داشت و
اندروید نصبِ نسخه‌ی جدید روی قبلی را رد می‌کرد — کاربر APK را نصب می‌کرد و
همان نسخه‌ی قدیمی را می‌دید. کلید یک بار ساخته شد (PKCS12، alias
`androiddebugkey`، پسورد `android` برای هر دو، اثر انگشت SHA-256 با
`1B:E5:CE:F4…` شروع می‌شود) و base64ش در secret مخزن
`DEBUG_KEYSTORE_BASE64` نشسته. **در ریپو کامیت نشده، چون ریپو عمومی است.**
اگر آن secret پاک شود، CI کلید موقت می‌سازد و APKش روی نصب‌های قبلی نمی‌نشیند.

**نسخه از شماره‌ی اجرای CI می‌آید:** `BUILD_NUMBER` (همان `github.run_number`)
در workflow ست می‌شود و `app/build.gradle.kts` از آن `versionCode` و
`versionName = "1.0.$buildNumber"` می‌سازد. قبلاً هر دو ثابت بودند (۱ و
«1.0»)، یعنی همه‌ی APKها برای اندروید یکسان به‌نظر می‌رسیدند. نسخه در پایین
صفحه‌ی «تنظیمات» نشان داده می‌شود تا معلوم باشد چه چیزی نصب شده.

**کامیت‌های فقط-مستندات دیگر بیلد نمی‌سازند** (`paths-ignore` برای `**.md` و
`مستندات/**`) — یک بار همین باعث شد شماره‌ی انتشار جلو برود و لینک اشتباه به
کاربر داده شود.

**قبل از فرستادن هر لینک APK، محتوایش را بازرسی کن.** APK یک زیپ است؛ با
خواندن دایرکتوری مرکزی‌اش می‌شود بدون نصب تأیید کرد که asset‌های تازه واقعاً
داخلش هستند. CI سبز فقط یعنی بیلد شد، نه اینکه فایل درست را فرستادی.
۳. مجوز بازنشرِ تفسیر نور (© مرکز فرهنگی درس‌هایی از قرآن) برای انتشار
   فروشگاهی روشن نیست — به کاربر گفته شده.
