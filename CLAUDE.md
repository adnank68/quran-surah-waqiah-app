# ختم سوره واقعه (quran-surah-waqiah-app)

اپ اندروید نیتیو (Kotlin/Compose) برای ختم سوره واقعه. قراردادهای مشترک همه‌ی
پروژه‌های این مجموعه (یک ریپو به‌ازای هر اپ، نبودن SDK محلی، قیدهای کافه‌بازار)
در `g:\myapps\game\CLAUDE.md` است، و موانع تکرارشونده‌ی ساخت APK در
`g:\myapps\game\APK-PLAYBOOK.md`. این فایل فقط چیزهای مخصوص همین پروژه را دارد.

## وضعیت — ۲۰۲۶-۰۹-۰۳

ریپو **عمومی** است و APK از GitHub Release خودِ همین ریپو منتشر می‌شود.
آخرین نسخه: `1.0.9` (`build-9`). هنوز روی کافه‌بازار منتشر نشده.

پروژه در ۲۰۲۶-۰۹-۰۲ از صفر ساخته شد. **ریپو قبلاً یک اسکلت React Native بود که
اصلاً قابل بیلد نبود** (نه پوشه‌ی `android/`، نه `index.js`، نه navigation، نه
متن واقعی قرآن) و کاملاً حذف و جایگزین شد. کامیت‌های اولیه‌ی تاریخچه‌ی گیت ربطی
به اپ فعلی ندارند.

زبان: کد و کامنت‌ها و مستندات **فارسی**، مثل `jadval-emrooz` و `dino` (نه مثل
`sudoku-pro` که انگلیسی است).

## امکانات فعلی

| بخش | جزئیات |
|---|---|
| متن سوره | ۹۶ آیه با خط عثمان‌طه، اندازه‌ی متن قابل تنظیم |
| ترجمه | انصاریان، الهی قمشه‌ای، قرائتی، مکارم — **تک‌انتخابی** |
| تلاوت | دو قاری، آیه‌به‌آیه، با ضربه روی آیه شروع می‌شود |
| برنامه‌ی ختم | ۴ روش نقل‌شده + برنامه‌ی دلخواه، چک‌باکس روزانه، حلقه‌ی پیشرفت |
| یادآور | ساعت دلخواه کاربر، `AlarmManager` |
| محتوا | روش‌های ختم با دعا، شأن نزول و فضایل، تفسیر نور، دعای پایان ختم |
| تم | روشن/تیره/طبق گوشی + ۶ پالت رنگ پس‌زمینه |
| شماره‌ی آیه | «بالای آیه» یا «کنار آیه» (انتخابی) |

## درسِ اصلی این پروژه: محتوا را از منبع بیاور، ننویس

هیچ متن قرآنی، ترجمه، تفسیر، روایت یا دستور ختمی در این اپ نوشته‌ی مدل نیست.
همه از منابع بیرونی نقل شده‌اند و منبع هرکدام هم داخل اپ (تنظیمات ← منابع
محتوا) و هم در `tools/README.md` ثبت است. ادامه‌ی مستقیمِ درسِ
`برنامه-ریزی-و-دستیار-شمسی` است (آنجا تبدیل تاریخ و مناسبت‌ها و موتور استخاره‌ی
تولیدشده با AI همگی غلط بودند).

**اگر بعداً محتوایی اضافه می‌شود: اول منبع، بعد متن.**

- **چیزی که پیدا نشد:** کاربر «حاج آقا عالی» را هم به‌عنوان منبع روش ختم نام
  برده بود؛ روش قابل‌استنادی به این نام پیدا نشد و عمداً چیزی از خودمان اضافه
  نشد. اگر منبعش را داد، به `KHATM_METHODS` در `tools/build-data.js` اضافه شود.
- **تفسیر فارسی API ندارد:** نه `api.alquran.cloud` و نه `api.quran.com` تفسیر
  فارسی سرو نمی‌کنند (چک شد). تفسیر نور از صفحه‌های تک‌آیه‌ی
  `wiki.ahlolbait.com` استخراج شد (`div.tafasir-tab-content`، تبِ فعال همان
  تفسیر نور است) و با هش، بخش‌های تکراری یکی شدند: ۹۶ آیه → ۱۸ بخش.
- **دعای پایان ختم** از سند Word خودِ کاربر آمده (`مستندات/`). `.docx` یک زیپ
  است؛ `word/document.xml` را با `zlib.inflateRawSync` باز کن و `<w:t>`ها را
  بچین — بدون هیچ وابستگی.

## داده

`app/src/main/assets/waqiah.json` (~۱۵۲ کیلوبایت) دستی نوشته نشده — با
`tools/build-data.js` ساخته می‌شود. دو فایل میانی (`tools/raw.json` و
`tools/tafsir_noor.json`) کامیت شده‌اند تا بازتولید به بالا بودن سایت‌ها وابسته
نباشد.

`build-data.js` پیش از نوشتن، خودش صحت را می‌سنجد و در صورت خطا با کد خروجی
غیرصفر می‌ایستد: ۹۶ آیه، پر بودن هر ۴ ترجمه، پوشش کامل تفسیر، جمع ۱۰۵ و ۴۱
برای دو ختم، نبودن نشانه‌های دایره‌ای و «ءَا»، و کامل بودن بندهای دعا.
`WaqiahContentTest` همان چیزی را که واقعاً داخل APK می‌رود دوباره می‌سنجد.

**تله‌ای که خورد و حل شد:** نسخه‌ی عثمانیِ `api.alquran.cloud`، «بسم الله» را
به آیه‌ی اول می‌چسباند. جداسازی با مقایسه‌ی رشته‌ی خام کار نمی‌کند (الف وصل `ٱ`
و الف خنجری)، و اگر فقط حروف پایه را تطبیق بدهی، کسره‌ی آخرِ «ٱلرَّحِيمِ» سرِ
آیه‌ی اول جا می‌ماند. `splitBismillah` بعد از تطبیق حروف پایه، اعرابِ چسبیده را
هم برمی‌دارد.

## تلاوت

**دو قاری، قابل انتخاب کاربر** — `audio/Reciter.kt`:

| قاری | منبع | حجم در APK |
|---|---|---|
| استاد منشاوی (مرتّل) | `everyayah.com/data/Minshawy_Murattal_128kbps/` | ۴٫۸۵ مگابایت |
| کریم منصوری (مرتّل) | `everyayah.com/data/Karim_Mansoori_40kbps/` | ۹٫۷۵ مگابایت |

فایل‌ها در `assets/audio/<folder>/000.mp3 … 096.mp3` می‌نشینند و نام‌گذاری هر دو
قاری **یکسان** است (`000` = بسم‌الله، `001..096` = آیات). به همین دلیل عوض کردن
قاری فقط مسیر فایل را عوض می‌کند. **افزودن قاری سوم = یک ورودی در `Reciter` و
یک پوشه‌ی جدید، بس.**

**چرا یک فایل به‌ازای هر آیه:** هماهنگی متن و صوت بدون هیچ جدول زمان‌بندی‌ای
درست درمی‌آید — آیه‌ی در حال تلاوت همان فایلی است که باز است و
`setOnCompletionListener` بعدی را باز می‌کند. دریفت معنا ندارد.

`audio/RecitationPlayer.kt`: `MediaPlayer` + مدیریت audio focus (با تماس ورودی
مکث می‌کند و خودکار ادامه نمی‌دهد). قاری جاری را خودِ پخش‌کننده نگه می‌دارد،
چون رفتن خودکار به آیه‌ی بعد داخل `setOnCompletionListener` است؛ `setReciter`
اگر وسط تلاوت باشد همان قطعه را با صدای جدید از سر می‌گیرد.
`WaqiahContentTest` وجود هر ۹۷ قطعه را **برای هر دو قاری** داخل APK می‌سنجد.

تلاوت با خروج از صفحه‌ی سوره قطع می‌شود (`DisposableEffect` در `QuranScreen`).

### بلندی صدا — یکسان‌سازی‌شده

کاربر گزارش داد صدای منصوری پایین است و اندازه‌گیری EBU R128 تأییدش کرد:
منشاوی −۱۴٫۱۸ در برابر منصوری −۲۸٫۵۳ LUFS، یعنی **۱۴٫۳۵ دسی‌بل** اختلاف.

هر دو با بهره‌ی **یکنواخت روی کل مجموعه** به حدود −۱۵ LUFS آورده شدند
(`volume=NdB,alimiter=limit=0.841`)؛ اختلاف نهایی ۰٫۶۸ دسی‌بل.

- **هر آیه را جدا نرمال نکن** — تفاوت طبیعی بلندی بین آیه‌ها از بین می‌رود و
  تلاوت مسطح می‌شود.
- رمزگذاری از فایل‌های **اصلی** انجام شد نه از نسخه‌ی فشرده‌ی قبلی، تا یک نسل
  افت کیفیت اضافه نشود. منصوری در ۶۴k رمزگذاری شد گرچه منبعش ۴۰k است، چون
  اعمال بهره روی همان ۴۰k آرتیفکت اضافه می‌کرد.

### چرا کیفیت منصوری بالاتر نرفت

نسخه‌ی **آیه‌به‌آیه**‌ی باکیفیت‌تری از او وجود ندارد: `everyayah` فقط همان ۴۰k
را دارد، و `alquran.cloud` (۱۹۰ نسخه‌ی صوتی) و `api.quran.com` اصلاً او را
ندارند.

ترتیل کاملش با ۱۲۸k استریو روی آرشیو اینترنت هست (آیتم `ffffff…07_20200906`،
«كريم منصوري مرتل مصحف مقسم اجزاء»، جزء ۲۷) ولی **یک فایل پیوسته‌ی ۵۶ دقیقه‌ای**
است. دو تلاش برای برش خودکارش به ۹۷ آیه انجام و **هر دو رد شد** — سنجه‌ی تصمیم
و دلیلش در `APK-PLAYBOOK.md` بند ۶٫۹٫۲.

اگر روزی دوباره سراغش رفتی: محل سوره در آن فایل حدود **۳۸:۲۶** است (مکث ۱٫۶۱
ثانیه‌ای، ۶۸٫۹٪ فایل، منطبق بر محل انتظاریِ ۶۸٫۷٪) و سوره‌ی حدید حدود ۵۲:۱۶.
ولی آن ضبط حدود **۳۵٪ تندتر** از نسخه‌ی آیه‌به‌آیه است، یعنی ضبط دیگری است و
مدت‌هایش برای هم‌ترازی خودکار به کار نمی‌آید. برش دستی و گوش‌دادن لازم است.

**محدودیتِ شناخته‌شده:** foreground service ندارد، پس اگر اپ مدت زیادی در
پس‌زمینه بماند ممکن است سیستم پخش را قطع کند. برای پخش پایدار باید
`MediaSessionService` اضافه شود.

## فونت‌ها — قاعده‌ای که نباید شکسته شود

دو فونت در `res/font/`: `uthmanic_hafs` (KFGQPC) و `vazirmatn`.

**خط قرآنی فقط برای متن خودِ قرآن.** فونت KFGQPC فقط برای ترکیب‌هایی طراحی شده
که در قرآن می‌آیند. متن دعاها رسم‌الخط فارسی دارد و مثلاً دنباله‌ی «ی + الف
خنجری» (`یٰا`، ۲۰ بار در دعای پایان ختم) در آن **لنگرِ چسباندن ندارد**؛ نتیجه
این بود که رندرر به‌جای هر «یٰ» یک دایره‌ی خالی می‌گذاشت و کل دعا خراب دیده
می‌شد.

- `ArabicText` + `ayahTextStyle` → فقط آیات و بسم‌الله (خط قرآنی)
- `DuaText` + `duaTextStyle` → همه‌ی دعاها (وزیرمتن)

**نکته‌ی دیگر فونت:** KFGQPC ارقام عربی و `U+06DD` را دارد ولی پرانتزهای تزئینی
`﴿ ﴾` را **ندارد** — به همین دلیل شماره‌ی آیه در حالت «کنار آیه» یک المان
Compose است، نه متنِ درج‌شده در رشته‌ی آیه.

## اصلاح رسم‌الخط

دو مشکل که فقط با اسکرین‌شات کاربر از دستگاه واقعی دیده شدند و در
`normalizeUthmani` داخل `tools/build-data.js` حل شده‌اند (۱۲ آیه عوض شد):

۱. **دایره‌های سفید کنار بعضی کلمه‌ها** — `U+06DF` (صفر گرد کوچک) و `U+06DE`
   (نشان رب‌الحزب، آیه‌ی ۷۵). فونت هر دو را **دارد**؛ مشکل این است که اندروید
   آن‌ها را به‌جای علامتِ ریزِ بالای حرف، گلیفِ مستقل با عرضِ خودش رندر می‌کند.
   حذف شدند — هیچ حرفی از متن کم نشد.
۲. **«ءَا» به‌جای «آ»** — رسم‌الخط Tanzil برای «آ» دنباله‌ی `ء + فتحه + ا`
   می‌نویسد. به `U+0622` تبدیل شد: `ٱلْءَاخِرِينَ` ← `ٱلْآخِرِينَ`.

هر دو تستِ واحد دارند تا برنگردند.

## نسخه‌ها و CI

الگو از `sudoku-pro` گرفته شده و همان ترکیبِ سبزِ آنجاست: AGP **9.1.1**،
Gradle **9.3.1** (AGP 9.1.1 حداقلِ سخت‌گیرانه دارد)، Kotlin **2.2.10**،
Compose BOM **2024.09.00**، JDK **17**، compileSdk/targetSdk 36، minSdk 24.

- افزونه‌ی `org.jetbrains.kotlin.android` اعمال **نمی‌شود** — AGP 9.x پشتیبانی
  Kotlin را داخل خودش دارد.
- `gradlew`، `gradle/wrapper/` و `debug.keystore` کامیت نمی‌شوند.
- **بدون Room/KSP و بدون Firebase** — عمداً، تا سطح خطای بیلد کم بماند. حالت اپ
  کوچک است و در `SharedPreferences` به‌صورت JSON می‌نشیند (`data/AppPrefs.kt`).
- Robolectric روی API 34 پین شده (`app/src/test/resources/robolectric.properties`)
  چون نسخه‌ی ۴٫۱۶ ایمیج API 36 ندارد.
- CI روی هر push به `main` هم artifact می‌گذارد و هم GitHub Release می‌سازد.
  ریپو عمومی است، پس برخلاف `sudoku-pro` به ریپوی انتشار جدا و `RELEASE_PAT`
  نیازی نیست.
- **کامیت‌های فقط-مستندات بیلد نمی‌سازند** (`paths-ignore` برای `**.md` و
  `مستندات/**`) — یک بار همین باعث شد شماره‌ی انتشار جلو برود و لینک اشتباه به
  کاربر داده شود.

## امضا و شماره‌ی نسخه

**کلید دیباگ ثابت است.** قبلاً workflow در هر اجرا با `keytool -genkeypair`
کلید تازه می‌ساخت، یعنی هر APK امضای متفاوتی داشت و اندروید نصبِ نسخه‌ی جدید
روی قبلی را رد می‌کرد — کاربر نصب می‌کرد و همان نسخه‌ی قدیمی را می‌دید. کلید یک
بار ساخته شد (PKCS12، alias `androiddebugkey`، پسورد `android` برای هر دو، اثر
انگشت SHA-256 با `1B:E5:CE:F4…`) و base64ش در secret مخزن
`DEBUG_KEYSTORE_BASE64` نشسته. **در ریپو کامیت نشده، چون ریپو عمومی است.** اگر
آن secret پاک شود، CI کلید موقت می‌سازد و APKش روی نصب‌های قبلی نمی‌نشیند.

**نسخه از شماره‌ی اجرای CI می‌آید:** `BUILD_NUMBER` (همان `github.run_number`)
در workflow ست می‌شود و `app/build.gradle.kts` از آن `versionCode` و
`versionName = "1.0.$buildNumber"` می‌سازد. قبلاً هر دو ثابت بودند (۱ و «1.0»)،
یعنی همه‌ی APKها برای اندروید یکسان به‌نظر می‌رسیدند. نسخه پایین صفحه‌ی
«تنظیمات» نشان داده می‌شود تا معلوم باشد چه چیزی نصب شده.

**قبل از فرستادن هر لینک APK، محتوایش را بازرسی کن.** APK یک زیپ است؛ با خواندن
دایرکتوری مرکزی‌اش می‌شود بدون نصب تأیید کرد که asset‌های تازه واقعاً داخلش
هستند. **CI سبز فقط یعنی بیلد شد، نه اینکه فایل درست به دست کاربر رسید.**

## نکته‌های پیاده‌سازی که دوباره کشفشان نکن

- **پیشرفت بر پایه‌ی تلاوت است نه روز.** در ختم چهارده‌روزه روز اول ۱ تلاوت و
  روز چهاردهم ۱۴ تلاوت دارد؛ اگر پیشرفت را روی تعداد روز حساب کنی، تیک روز اول
  و روز چهاردهم یک اثر می‌گذارند. `KhatmPlanTest` این را قفل کرده است.
- **`perDay` قلبِ مدل است.** هر روش ختم فقط یک آرایه‌ی «تعداد تلاوت هر روز»
  است؛ طولش = تعداد روز، جمعش = کل. الگوهای عجیب (۳ بار هر شب ولی ۸ بار شب
  جمعه، پنج هفته) بدون هیچ منطق ویژه‌ای در همین آرایه جا می‌شوند.
- **تقویم شمسی**: الگوریتم برکوفسکی روی JDN، نه فرمول تقریبی ۳۳ساله. یک بار
  پیمانه‌ی ماه در `jdnToGregorian` عدد ۴ نوشته شده بود به‌جای ۱۲؛ چهار تاریخِ
  نمونه از پنج‌تا درست جواب می‌دادند و مشکل را پنهان می‌کردند — تستِ
  رفت‌وبرگشتِ روزبه‌روز آن را گرفت.
- **یادآور**: `AlarmManager` با `setExactAndAllowWhileIdle` و در نبودِ اجازه،
  برگشت به `setAndAllowWhileIdle`. آلارم دقیق تکرارشونده نیست، پس
  `ReminderReceiver` بعد از هر شلیک زنگ فردا را می‌گذارد. `nextTriggerMillis`
  اگر ساعت دقیقاً همین لحظه باشد فردا را برمی‌گرداند، وگرنه حلقه‌ی بی‌پایانِ
  شلیک درست می‌شود (تست دارد).
- **نوار پخش باید `navigationBarsPadding` داشته باشد** — با `enableEdgeToEdge`
  بدون آن زیر دکمه‌های ناوبری گوشی می‌افتد و ضربه‌ها به نوار سیستم می‌رسند،
  یعنی دکمه عملاً کار نمی‌کند.
- **آیکون**: minSdk 24 است و adaptive-icon فقط از API 26 کار می‌کند، پس علاوه بر
  `mipmap-anydpi-v26/` یک وکتور ساده هم در `mipmap/` هست. اگر برداری، روی
  اندروید ۷ آیکون ندارد.
- **RTL** با `CompositionLocalProvider(LocalLayoutDirection provides Rtl)` در
  `MainActivity` قفل شده، نه با زبانِ دستگاه — اپ همیشه فارسی است.
- **تم بیرون از ViewModel ساخته نمی‌شود**: `WaqiahTheme` از وضعیت ViewModel
  می‌خواند، پس ViewModel باید **بیرون** از `WaqiahTheme` ساخته شود.
- **متن آیه وسط‌چین، متن دعا راست‌چین** — دعاها متن پیوسته‌اند و وسط‌چین کردنشان
  بد خوانده می‌شود.

## کارهای باقی‌مانده پیش از انتشار روی کافه‌بازار

۱. **`applicationId`** الان `ir.almasbu.waqiah` است — بعد از اولین انتشار
   غیرقابل‌تغییر می‌شود، پس باید تأیید نهایی کاربر گرفته شود.
۲. **کلید امضای انتشار ساخته نشده.** کلید فعلی یک کلید دیباگِ ثابت است و
   نسخه‌ی release هم با همان امضا می‌شود تا APK قابل نصب باشد. برای انتشار
   واقعی باید یک keystore دائمیِ جدا ساخته شود و در secret بنشیند (الگوی
   `sudoku-pro`؛ تله‌ی PKCS12 در `APK-PLAYBOOK.md` بند ۴٫۲).
۳. **مجوز بازنشرِ تفسیر نور** (© مرکز فرهنگی درس‌هایی از قرآن) و **تلاوت‌ها**
   برای انتشار فروشگاهی روشن نیست — به کاربر گفته شده.
۴. آیکون و تصاویر فروشگاه آماده نشده.
